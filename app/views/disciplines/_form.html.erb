<%= simple_form_for @discipline do |f| %>

   <%= render "shared/error_messages", :record => @discipline %>

   <p>      
      <%= f.input :name, requered: true %>
   </p>

   <p>
      <%= f.button :submit %>
   </p>
<% end %>